leer las teoarias principalmente casi todo esta ahi

los id solo va en clases que se desea que persistan

revisar las relaciones en hbm.xml y las colecciones(ejemplo conductores) se escriben de distinta manera que las simples(ejemplo nombre), buscar en teorias

el la configuracion (cfg.xml) se deben incluir los hbm.xml, todos sino no funca

en las clases deben estar en un package con nombre no por defauld como esta ahi 
